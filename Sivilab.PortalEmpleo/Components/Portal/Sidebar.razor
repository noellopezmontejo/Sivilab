<style>
    .sidebar {
        background-color: #ffffff;
        border-radius: 10px;
        padding: 2rem 1.5rem;
        box-shadow: 0 4px 8px rgba(0, 64, 128, 0.15), 0 0 10px rgba(0, 0, 0, 0.05);
        border-left: 6px solid var(--primary-color);
        flex-shrink: 0;
        height: fit-content;
        align-self: flex-start;
    }


        .sidebar h3 {
            margin-bottom: 1.5rem;
            font-size: 1.3rem;
            color: var(--primary-color);
            font-weight: 700;
            letter-spacing: 0.03em;
        }

        .sidebar ul {
            list-style: none;
            padding-left: 0;
        }

        .sidebar li {
            margin-bottom: 1.1rem;
        }

        .sidebar a {
            text-decoration: none;
            color: var(--secondary-color);
            font-weight: 600;
            font-size: 1rem;
            transition: color 0.3s ease;
        }

            .sidebar a:hover {
                color: var(--primary-color);
                text-decoration: underline;
            }
    @@media (max-width: 812px) {
        .sidebar {
            width: 100%;
            padding: 1rem;
            margin-bottom: 2rem;
        }
    }
</style>

<aside class="sidebar">
    <h3>Accesos rápidos</h3>
    <ul>
        <li><a href="#registro-curp">Buscar CURP</a></li>
        <li><a href="#ofertas">Ofertas destacadas</a></li>
        <li><a href="#eventos">Próximos eventos</a></li>
        <li><a href="#programas">Programas de empleo</a></li>
    </ul>
</aside>

@code {}